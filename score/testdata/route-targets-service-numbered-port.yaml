kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: console
spec:
  host: console-openshift-console.apps.xyz01.example.com
  to:
    kind: Service
    name: console
    weight: 100
  port:
    targetPort: 8443
  wildcardPolicy: None